import { RemarkRelatedData } from "../../games/wbm";

// this is to separate certain remarks from others, for formatting reason
// see example here: https://b.cgas.io/zeyy__GzR5x9.png
export async function execute(data: RemarkRelatedData): Promise<string> {
  return "\n\n";
}

export const index = 89;

export const disabled = false;
